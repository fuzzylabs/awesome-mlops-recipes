apiVersion: v1
kind: Service
metadata:
  name: pr-review-agent
  namespace: agent
  labels:
    app: pr-review-agent
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: pr-review-agent

