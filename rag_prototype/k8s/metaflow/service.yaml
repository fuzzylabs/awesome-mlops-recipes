apiVersion: v1
kind: Service
metadata:
  name: metaflow
  namespace: metaflow
  labels:
    app: metaflow
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: metaflow
