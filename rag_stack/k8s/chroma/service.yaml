apiVersion: v1
kind: Service
metadata:
  name: chroma
  namespace: chroma
  labels:
    app: chroma
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  selector:
    app: chroma
